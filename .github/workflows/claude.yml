name: Ritense PR examinator

on:
  pull_request:
    branches:
      - main
    paths:
      - 'frontend/projects/plugin/**'
      - 'backend/plugin/**'
      - 'build.gradle.kts'
      - 'settings.gradle.kts'
      - 'valtimo-configurator-metadata.json'
      - '!**/*.md'
jobs:
  # Determine whether an Anthropic API key is configured for this repository.
  # The Claude review is optional: repositories without ANTHROPIC_API_KEY simply
  # skip it instead of getting a failing PR check.
  check-secret:
    runs-on: ubuntu-latest
    outputs:
      has_key: ${{ steps.check.outputs.has_key }}
    steps:
      - name: Check for ANTHROPIC_API_KEY
        id: check
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          if [ -n "$ANTHROPIC_API_KEY" ]; then
            echo "has_key=true" >> "$GITHUB_OUTPUT"
          else
            echo "has_key=false" >> "$GITHUB_OUTPUT"
            echo "ANTHROPIC_API_KEY is not configured; skipping Claude PR review." >> "$GITHUB_STEP_SUMMARY"
          fi

  PR_Ritense:
    needs: check-secret
    if: ${{ needs.check-secret.outputs.has_key == 'true' }}
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      issues: write
      id-token: write
      actions: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 1

      - name: "⏳ Post review started comment"
        id: progress_comment
        uses: actions/github-script@v7
        with:
          script: |
            const body = [
              '## 🤖 Claude Review — In Progress',
              '',
              '| Step | Status |',
              '|------|--------|',
              '| 📥 Checkout          | ✅ Done   |',
              '| 🔍 Analysing diff    | ⏳ Running |',
              '| 💬 Posting feedback  | ⏳ Pending |',
              '',
              '_Claude is reviewing your changes. This comment will be updated when complete._',
            ].join('\n');
            
            const pr_number = context.payload.pull_request?.number
              ?? context.payload.issue?.number;
            
            const comment = await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: pr_number,
              body,
            });
            
            core.setOutput('comment_id', comment.data.id);
            core.setOutput('pr_number', pr_number);

      - name: PR Review with Progress Tracking
        id: claude_review
        uses: anthropics/claude-code-action@v1
        with:
          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
          path_to_claude_code_executable: ${{ env.CLAUDE_PATH }}
          track_progress: true
          claude_args: |
            --max-turns 20
            --model claude-sonnet-4-6
          prompt: |
            REPO: ${{ github.repository }}
            PR NUMBER: ${{ github.event.pull_request.number }}
            
            Please review this pull request with plugin-pr-reviewer agent.

            Note: The PR branch is already checked out in the current working directory.
      - name: "✅ Update progress comment (success)"
        if: success()
        uses: actions/github-script@v7
        with:
          script: |
            const body = [
              '## 🤖 Claude Review — Complete ✅',
              '',
              '| Step | Status |',
              '|------|--------|',
              '| 📥 Checkout          | ✅ Done |',
              '| 🔍 Analysing diff    | ✅ Done |',
              '| 💬 Posting feedback  | ✅ Done |',
              '',
              `_Review completed at ${new Date().toUTCString()}_`,
            ].join('\n');

            await github.rest.issues.updateComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              comment_id: ${{ steps.progress_comment.outputs.comment_id }},
              body,
            });

      - name: "❌ Update progress comment (failure)"
        if: failure()
        uses: actions/github-script@v7
        with:
          script: |
            const body = [
              '## 🤖 Claude Review — Failed ❌',
              '',
              '| Step | Status |',
              '|------|--------|',
              '| 📥 Checkout          | ✅ Done    |',
              '| 🔍 Analysing diff    | ❌ Failed  |',
              '| 💬 Posting feedback  | ⏳ Skipped |',
              '',
              '**The review encountered an error.** Check the [Actions log](' +
              `https://github.com/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}` +
              ') for details.',
              '',
            ].join('\n');

            await github.rest.issues.updateComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              comment_id: ${{ steps.progress_comment.outputs.comment_id }},
              body,
            });
