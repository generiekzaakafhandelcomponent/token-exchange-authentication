# Getting Started

This repository contains the `token-exchange-authentication` plugin, its backend module
(`backend/plugin`), and the example application (`backend/app`, `frontend`) used to run and test
it locally.

For more information on how to build a plugin, see
the [Custom Plugin Definition](https://docs.valtimo.nl/features/plugins/plugins/custom-plugin-definition) documentation.
